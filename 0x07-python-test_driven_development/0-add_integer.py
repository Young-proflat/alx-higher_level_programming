#!/usr/bin/python3
""" add two integer together"""
def add_integer(a, b=98):
    if type(a) is not an int or type(a) is not float:
        raise TypeError('a must be an integer')
    if type(b) is not an int or type(b) is not float:
        raise TypeError('b must be an integer')
return (int(a) + int(b))
